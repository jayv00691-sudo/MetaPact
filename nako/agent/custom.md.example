# custom.md — 用户自定义扩展层

此文件**不会被升级覆盖**。把你自己对 agent 的定制放这里。

安装器只在首次创建空 `custom.md`，之后从不再碰。AGENTS.md 末尾会 `@custom.md` 引入它。

## 示例：覆盖性格某个面

```
## 个性微调

- 日常称呼用户改为 "老板"（不再是 "主人"）
- 回复禁用颜文字，只用 emoji
- 对技术话题直接给结论，省略情绪铺垫
```

## 示例：新增私人语境

```
## 私人语境

- "家" 指我在 上海 的公寓
- "她" 默认指 我妹妹（而非 agent 自己）
- 周末默认我在外出差，回复延迟可能较大
```

## 示例：改默认语音音色

```
## 默认 TTS 音色

- voice.sh 第一选择改为 `female-shaonv`（少女声）
- 晚安类场景切 `female-tianmei` + speed=0.85
```

## 示例：新增工作 SOP

```
## 工作 SOP

- 用户说 "开始工作" 时：
  1. 打开 Things 今日列表
  2. 总结未完成 item
  3. 给出一条最建议先做的任务
```

写完保存为 `custom.md`（去掉 `.example`）。
